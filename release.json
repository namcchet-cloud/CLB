{
  "version": "7.0.7",
  "name": "CLB v7.0.7 Touch + PWA Pipeline",
  "minimumNode": "18",
  "notes": "Improves smartphone record-player touch handling with a quick-place fallback, verifies the gallery remains WebP-only with lazy/async decoding, completes social preview metadata, and adds a versioned Service Worker for repeat-visit caching without changing the visual design."
}
