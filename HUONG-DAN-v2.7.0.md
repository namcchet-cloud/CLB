# CLB Nghệ thuật — v2.7.0

Bản này tập trung vào hai khu vực: Gallery Haruko và Music.

- Dây leo cũ được thay bằng hệ cành gỗ + rễ leo đan xen phủ toàn khu triển lãm, không còn chỉ bám trong từng card.
- Tử đằng, lá, hoa hồng nhỏ, sakura, lilac và hoa trắng được vẽ bằng SVG/CSS; hiệu ứng chỉ hoạt động khi Haruko được chọn và Gallery nằm trong viewport.
- Máy đĩa được thu nhỏ còn khoảng 300px và vẫn giữ khung vuông.
- Hai bìa album xếp chồng cạnh máy, lộ gáy có tên và thông số LP · 33⅓ RPM · Spotify.
- Có thể chạm hoặc kéo bìa album; animation sẽ đưa bao album tới máy, rút đĩa ra rồi đặt xuống mâm. Đĩa không tự phát nhạc; chỉ quay khi người dùng bấm Phát và Spotify phản hồi trạng thái phát.
- Có chỉ dẫn nhỏ ngay trên album và dưới máy.

## Cập nhật GitHub
Ghi đè cùng lúc `index.html`, `style.css`, `script.js`, `content.js`. Giữ nguyên thư mục `assets/` hiện có.

Sau deploy, mục Chuyển động phải hiển thị `v2.7.0`.
